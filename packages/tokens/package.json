{
  "name": "@digdir/ds-tokens",
  "version": "1.5.0",
  "private": false,
  "description": "Design tokens for DigDir design system",
  "author": "DigDir design system team",
  "license": "BSD-3-Clause",
  "main": "./build/tokens.css",
  "files": [
    "/build"
  ],
  "scripts": {
    "build": "yarn clean && style-dictionary build --config ./style-dictionary.config.js",
    "clean": "rm -rf ./build/"
  },
  "devDependencies": {
    "style-dictionary": "^3.0.2"
  }
}
