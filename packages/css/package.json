{
  "name": "@digdir/ds-admin-css",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "yarn clean && sass --no-source-map --style=compressed src/index.scss build/index.css",
    "clean": "rm -rf ./build/"
  },
  "dependencies": {
    "@digdir/ds-tokens": "^1.3.0"
  },
  "devDependencies": {
    "sass": "^1.42.1"
  }
}
