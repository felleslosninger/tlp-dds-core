{
  "name": "@digdir/ds-core-css",
  "version": "0.5.0",
  "private": false,
  "description": "CSS core of the Digdir design system, including stylesheets for the baseline and core components",
  "author": "Digdir design system team",
  "license": "BSD-3-Clause",
  "main": "./build/index.css",
  "files": [
    "/build"
  ],
  "scripts": {
    "build": "yarn clean && sass --no-source-map --style=compressed --load-path=../../node_modules/ src/index.scss build/index.css && cp -R -v src/ build/src/",
    "clean": "rm -rf ./build/"
  },
  "dependencies": {
    "@digdir/ds-tokens": "^1.6.2"
  },
  "devDependencies": {
    "sass": "^1.42.1"
  },
  "publishConfig": {
    "access": "public"
  }
}
